# Local Runtime

This copy has not been configured for a specific machine.

- `RRC_COMMAND`: `rrc`

If `rrc` is unavailable, run the package setup command before using this skill:

```bash
rrc setup --agent both
```

An installation created by `rrc setup` replaces this file with the exact isolated Python runtime path.
